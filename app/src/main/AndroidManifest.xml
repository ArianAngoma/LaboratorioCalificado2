<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angoma.arianpc.laboratoriocalificado">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="sugarormapp.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.angoma.arianpc.laboratoriocalificado.models" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignupNow" />
        <activity android:name=".activities.FragmentsActivity" />
        <activity android:name=".activities.item_notes" />
        <activity android:name=".activities.AddActivity" />
        <activity android:name=".activities.StoreActivity" />
        <activity android:name=".activities.QuizActivity" />
        <activity android:name=".activities.ExploreActivity"></activity>
    </application>

</manifest>